# Sidecar bundle for config.jelly (same pattern as matrix-project-plugin per-view *.properties).
axisNames=Axis names (optional)
axisNamesDescription=Comma-separated axis names in priority order (for example: BROWSER, PLATFORM). If empty, sorter falls back to the last axis as before.
